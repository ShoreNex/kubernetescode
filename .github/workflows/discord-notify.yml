name: Notify Discord on Push

on:
  push:
    branches:
      - main  # for the main branch

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
    - name: discord-webhook
      uses: johnnyhuy/actions-discord-git-webhook@v1.0.1
      with:
        webhook_url: ${{ secrets.DISCORD_WEBHOOK }}  # Correct input key
        message: 'A new commit has been pushed to the repository! Commit message: "${{ github.event.head_commit.message }}".'  # Keep this as is
